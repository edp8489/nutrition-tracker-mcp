# Contributing

The GitHub repo serves as a public mirror of the `release` branch only. Pull requests for bug fixes are always welcome. 

If you'd like to get involved with future development, reach out to `contact@epeters.io` for access to the dev branches.